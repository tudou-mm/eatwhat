# ---------- 环境与凭证（最重要，绝不能提交） ----------
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.jks
credentials.json
secrets.yml
application-local.yml
application-prod.yml

# 高德地图 Key（按 Key 维度算配额，泄露=送额度）—— 正式 Key 只留本地
assets/data/amap-key.js
amap-key.js

# ---------- ⚠️ 发布沙箱的排除清单（.gitignore 管不住它） ----------
# 发布工具只按它自己的规则排除 node_modules/.git/构建产物，
# **不读 .gitignore** → gitignored 的敏感文件照样会被打包上传。
# 想让某个文件不上线，必须显式禁止发布目录（见 .publishignore 说明）：
#   · 发布前把敏感文件移出发布目录，或只发一个干净的子目录
#   · 绝不要把仓库根整个当发布目录
.wbapp_*.genie

# ---------- Java / Maven ----------
server/target/
*.class
*.jar
*.war
!.mvn/wrapper/maven-wrapper.jar
.mvn/timing.properties

# ---------- IDE ----------
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.swp
*.swo
.settings/
.project
.classpath

# ---------- 系统 ----------
.DS_Store
Thumbs.db
desktop.ini

# ---------- 日志与临时文件 ----------
*.log
logs/
*.tmp
*.bak
*~

# ---------- 测试截图（drive_*.py 产出，本地看效果用） ----------
.shots/
.shots-admin/
.shots-merchant/
.shots-flow/
.shots-v2/
.shots-mappicker/
.shots-loc/
.shots-host/
.dev.log

# ---------- 数据库本地文件 ----------
*.db
*.sqlite
*.sqlite3
server/data/

# ---------- 用户上传的媒体（POST /api/upload 落盘到这里，不进仓库） ----------
server/uploads/
uploads/

# ---------- Node（如果以后加前端构建） ----------
node_modules/
dist/
.cache/

# ---------- Python（server/tools 下的脚本跑起来会生成） ----------
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ---------- 种子生成的中间产物 ----------
# server/tools/_gen/ 是 radar_to_seed.py 的中间产物（约 1MB JSON），
# 内容可由 data/radar.db 完整重建 → 不进仓库，换机跑一遍脚本即可。
# ⚠️ 但**最终产物 server/src/main/resources/seed/shops.json 必须提交**
#    （DataSeeder 启动时读它，缺了就只能退到内置的 22 家演示店）。
server/tools/_gen/

# ---------- WorkBuddy 本地状态 ----------
.workbuddy/
